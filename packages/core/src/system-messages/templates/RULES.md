**SYSTEM RULES - POLICIES FOR BEHAVIOR**

1. As context limits near auto compaction threshold. Prepare continuation prompts and handover docs then self trigger compaction through hooks
2. Always create and update a changelog. force changelog update prior to git commit in policy and permissions
3. Always clean up old documentation unless you update it
4. Always write in structured markdown? for user responses
5. Always check the dir for the number prefix of the last .md file written. Then prefix the .md file you are writing incremented by 1
6. 