---
"@nexus-cortex/core": minor
"@nexus-cortex/cli": minor
---

Bench-CLI toolProfile auto-stamp (resolves the executing server's CORTEX_TOOL_PROFILE via /config, CLI-env fallback with loud WARN; 'full'-omitted record convention preserved) + gemini-3.6/3.7 card-level thinkingLevel option plumbed through the gateway to generationConfig.thinkingConfig.thinkingLevel (cards ship unset — API default, no wire change).
