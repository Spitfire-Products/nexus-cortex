---
"@nexus-cortex/core": patch
---

grok-build ports (2026-08-01): request-copy age-tiered tool-result pruning (>50% utilization gate, defers compaction, canonical history untouched); context-length retry veto (overflow is never retryable — strict phrasing list); raw-fetch "(NNN):" status extraction (xAI/Google raw-fetch 429/5xx were never retried); rate-limit-specific retry cap (rateLimitMaxRetries=2); anti-polling clause in the Bash description; "one approval is not a blank check" in WORK_QUALITY.
