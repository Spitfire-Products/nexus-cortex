---
"nexus-canon": minor
---

feat(canon): the tool-ontology ladder, rung 1 — cross-harness tool compatibility. `canon pull` now prints a compatibility report for the receiving harness (native / name-mapped via the concept table / MCP / unmapped — with the doctrine note that comprehension is never capability-bound, since canon stores tool results verbatim); `canon tools` derives the observed tool inventory per harness from the canonical line itself and exposes the empirically-seeded cross-harness concept map (`TOOL_CONCEPTS`, 14 concepts across four harnesses).
