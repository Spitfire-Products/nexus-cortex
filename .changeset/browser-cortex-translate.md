---
"nexus-canon": minor
---

browser-cortex translate branch. `/native/browser-cortex/` sessions — captured by
the Nexus Terminal SPA's CanonSyncService from browser CORTEX's IndexedDB, already
serialized into the canonical envelope — now translate into the canonical line via
the identity branch shared with nexus-cortex: verbatim carry + `provenance` stamp
plus the shared structural repairs (wrapped-block normalization, duplicate-result
drop, orphan-result synthetic repair). Projection refs materialize like every other
harness. Fifth captured harness; store docs (`MAPPING.md`) updated.
